// =========== Operators

let a = 10
let b = 5
let c = 32
// c = c + a
// c = c - a
// c = c * a
// c = c / a

// c += a
// c -= a
// c *= a
// c /= a

// c = 4 ** 2

// =========== Data types

const age = 18 // numbers
const name = 'art' //  srting
const isProgrammer =  true // boolean
let x // undefined
// null
// console.log(typeof (1/x))
// console.log(typeof NaN)

// ============ Prority
const fullAgge = 18
const birthYear = 2004
const currentYear = 2023
// > < >= <=
const isFullAge = currentYear - birthYear >= fullAgge
// console.log(isFullAge)

const num1 = 42
const num2 = '42'

console.log(num1 === num2) 